# Legacy SQL Directory

No executable tracked migrations live here anymore.

Use the canonical tracked migration sets under:

- `apps/web/migrations/main`
- `apps/web/migrations/phi-vault`

Bootstrap a fresh environment with:

- `npm run migrate:bootstrap`

This directory is kept only as a marker that old operator-run `.sql` files were retired.
